{
  "name": "takereview",
  "version": "1.0.0",
  "description": "TakeReview project - Datashake clone for fetching reviews from Amazon URLs",
  "scripts": {
    "start": "npm-run-all --parallel start-client start-server",
    "start-client": "npm --prefix client run dev",
    "start-server": "npm --prefix server run start"
  },
  "devDependencies": {
    "nodemon": "^3.1.9",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "amqplib": "^0.10.5",
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "find-config": "^1.0.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^6.9.16",
    "pg": "^8.13.1",
    "sequelize": "^6.37.5"
  }
}
